<div id="move_{{ move.id }}">
    {% set video = move.getVideo() %}
    {% if video %}
        {% include 'FrontFrontBundle:Video:show.html.twig' with { video : video } %}
    {% endif %}
    <span>{{ move.getTitle() }}</span>
</div>