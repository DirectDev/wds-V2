<h3>{% trans %}user.search_by_musictype{% endtrans %}</h3>
<ul class="list-unstyled">
    {% for musicType in musicTypes %}
        <li>
            <a href="{{ path('front_user_filter_events', {'event_filter[musictype]':  musicType.getName() , 'event_filter[_token]' :  csrf_token('event_filter')  } ) }}"
               class="search-user-musictype-link">
                {{ musicType.getTitle() }}
            </a>
        </li>     
    {% endfor %}   
</ul>
<hr>