connect:
    confirm:
        cancel: connect.confirm.cancel
        submit: connect.confirm.submit
        text: connect.confirm.text

    registration:
        cancel: connect.registration.cancel
        submit: connect.registration.submit

header:
    connecting: header.connecting
    register: header.register
    registration_success: header.registration_success
    success: header.success
